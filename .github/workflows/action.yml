name: Feature Branch
on:
  push:
    branches:
      - 'main'
jobs:
  test:
    name: Unit-Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK
        uses: actions/setup-java@v1
        with:
          java-version: 14-ea
      - name: Maven Package
        run: |
        cd tokenSpringSecurity
        mvn clean package
      - name: Maven Verify
        run: |
        cd tokenSpringSecurity
        mvn clean verify -DskipTests